@props(['titulo', 'subtitulo'])

<div class="p-5 text-center bg-dark text-white rounded mb-5 mt-4 shadow">
    <h1 class="mb-3 fw-bold">{{ $titulo }}</h1>
    <p class="lead">{{ $subtitulo }}</p>
</div>